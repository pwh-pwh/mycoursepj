spring:
  application:
    name: system
server:
  port: 9001
  servlet:
    context-path: /system
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/